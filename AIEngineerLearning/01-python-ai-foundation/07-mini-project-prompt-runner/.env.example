# Directory where prompt_runs.json will be stored.
# Relative and absolute paths are both supported.
PROMPT_RUNNER_OUTPUT_DIRECTORY=outputs

# JSON storage file name.
PROMPT_RUNNER_STORAGE_FILE=prompt_runs.json

# Maximum time to wait for another process to release the storage lock.
PROMPT_RUNNER_LOCK_TIMEOUT_SECONDS=10